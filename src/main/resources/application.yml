spring:
  server:
    port: 8081
    address: 0.0.0.0
  datasource:
    url: jdbc:mysql://db:3306/testdatabase
    username: testuser
    password: testpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
